import { browserName } from 'dummy/helpers/browser-name';
import { module, test } from 'qunit';

module('Unit | Helper | browser name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = browserName([42]);
  assert.ok(result);
});
